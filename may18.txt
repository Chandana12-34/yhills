Analyse my Streamlit frontend issue carefully and correct the rendering problem in the “Component details” section.

Current Issue:
- The first endpoint table renders correctly in proper HTML table format.
- The second table (“Component details”) is showing raw HTML tags like:
  <thead>
  <tbody>
  <tr>
  <td>
  instead of rendering as a proper table.
- Some rows render partially at the bottom while remaining HTML appears as plain text above.
- This indicates malformed HTML structure or incorrect Streamlit markdown rendering.

Project Context:
- Using Streamlit
- Using st.markdown(..., unsafe_allow_html=True)
- Building HTML tables dynamically using Python f-strings
- comp_rows contains dynamically generated <tr> rows
- Endpoint table works correctly
- Component details table does not

Please check for ALL possible frontend issues including:
1. Broken HTML structure
2. Missing <table> opening tag
3. Incorrect placement of <thead>, <tbody>, or </table>
4. Improper multiline f-string formatting
5. HTML escaping by Streamlit
6. Incorrect usage of st.markdown()
7. Using body= parameter incorrectly
8. Broken quote usage inside f-strings
9. Nested triple quote issues
10. Invalid HTML generated inside badge span
11. Extra closing tags
12. Incorrect indentation breaking the HTML string
13. Multiple st.markdown() calls splitting one table
14. Improper comp_rows generation

Requirements:
- Render Component details exactly like the first compact table
- Use only ONE st.markdown() call for the full component table
- Ensure HTML renders correctly without showing raw tags
- Preserve compact styling and alignment
- Keep status badges working properly
- Ensure all rows appear inside the table correctly

Provide:
1. Root cause analysis
2. Corrected render_components_table() function
3. Correct comp_rows generation
4. Correct st.markdown() usage
5. Final working Streamlit HTML rendering code

Expected Result:
A properly aligned compact HTML table for Component details with columns:
- Endpoint
- Component
- Status

No raw HTML tags should appear on screen.