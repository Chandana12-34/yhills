# ----- User Issue Analysis -----
    dash_ws.write(row, 0, "User Issue Analysis", title_fmt)
    row += 1

    user_issue_summary = dashboard["user_issue_summary"].head(20)

    user_issue_summary.to_excel(
        writer,
        sheet_name="Dashboard",
        startrow=row,
        startcol=0,
        index=False
    )

    row += len(user_issue_summary) + 4


    # ----- Champion Issue Analysis -----
    dash_ws.write(row, 0, "Champion Issue Analysis", title_fmt)
    row += 1

    champion_issue_summary = dashboard["champion_issue_summary"].head(20)

    champion_issue_summary.to_excel(
        writer,
        sheet_name="Dashboard",
        startrow=row,
        startcol=0,
        index=False
    )

    row += len(champion_issue_summary) + 4