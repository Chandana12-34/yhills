Analyse my Spring Boot actuator health response structure carefully and enhance my Streamlit Health Check Dashboard frontend.

Current Dashboard:
- I already fetch endpoint/component data from actuator /health APIs
- Each endpoint has a separate component table
- Columns currently are:
  1. Component
  2. Status

New Requirement:
Add a THIRD column called:
- Details

The actuator JSON contains nested component details like:

"db": {
    "status": "UP",
    "details": {
        "database": "Oracle",
        "validationQuery": "isValid()"
    }
}

"diskSpace": {
    "status": "UP",
    "details": {
        "total": 78671253504,
        "free": 63549206528,
        "threshold": 10485760,
        "path": "/",
        "exists": true
    }
}

Some components also contain nested child components:

"discoveryComposite": {
    "status": "UNKNOWN",
    "components": {
        "discoveryClient": {
            "status": "UNKNOWN",
            "description": "Discovery Client not initialized"
        }
    }
}

Requirements:
1. Add a new “Details” column in every component table
2. Extract and display component details dynamically from actuator JSON
3. If component.details exists:
   - Convert the details dictionary into readable key-value format
   - Example:
     database=Oracle
     validationQuery=isValid()

4. If nested child components exist:
   - Flatten and display useful information
   - Example:
     discoveryClient: Discovery Client not initialized

5. If NO details exist:
   - Display "-"
   
6. Keep separate table per endpoint:
   - E2E Tracker
   - Process Controller
   - Case Manager
   - etc.

7. Keep columns:
   - Component
   - Status
   - Details

8. Preserve existing status badge styling:
   - UP → green
   - DOWN → red
   - UNKNOWN → yellow/orange

9. Use clean compact enterprise dashboard UI
10. Ensure HTML renders correctly in Streamlit
11. Use only ONE st.markdown() call per endpoint table
12. Use proper HTML structure:
    - <table>
    - <thead>
    - <tbody>

13. Handle all actuator variations safely:
    - missing details
    - missing status
    - nested components
    - nested descriptions
    - empty dictionaries

14. Escape HTML properly to avoid broken rendering

Please provide:
1. Updated actuator parsing logic
2. Efficient helper function to extract details text
3. Updated component row generation logic
4. Final render_components_table() implementation
5. Streamlit-compatible HTML rendering code
6. Clean and readable table formatting

Expected UI:

------------------------------------------------------
E2E TRACKER ✅

| Component          | Status  | Details |
|-------------------|----------|----------|
| db                | UP       | database=Oracle, validationQuery=isValid() |
| diskSpace         | UP       | total=78671253504, free=63549206528 |
| discoveryClient   | UNKNOWN  | Discovery Client not initialized |
| readinessState    | UP       | - |
------------------------------------------------------

The solution should be production-quality, robust, and fully compatible with Streamlit.